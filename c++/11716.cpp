#include <bits/stdc++.h>
using namespace std;
int findRoot(int n) {
	int sqroot = (int) sqrt(n);
	if (sqroot * sqroot == n)
		return sqroot;
	return 0;
}

int main(int argc, char **argv) {

	return 0;
}
